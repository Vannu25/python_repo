import os
import sys

# sample_data = input("Input some comma separated numbers : ")
#
# list1 = sample_data.split(',')
# print(list1)
# tup = tuple(list1)
# print(tup)

# prog to check python version

cmd = sys.version_info and sys.version
print(cmd)
